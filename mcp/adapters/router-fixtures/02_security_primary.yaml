name: "Security Auditor \u2192 sast + secrets + headers (all primary)"
input:
  agent: security-auditor
  requested_capabilities:
    - security.scan
    - security.secrets
    - header.csp
expected:
  allowlist:
    - semgrep
    - gitleaks
    - fetch
  secondary_candidates: []
